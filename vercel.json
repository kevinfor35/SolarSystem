{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "framework": "vite",
  "installCommand": "npm install",
  "ignoredBuildStep": "[ -z \"$(git diff HEAD~1 --name-only | grep -v -E '\\.(md|txt)$')\" ] && exit 0 || exit 1"
}